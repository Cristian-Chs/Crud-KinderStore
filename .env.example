# Configuración Local para Pruebas con PostgreSQL

# IMPORTANTE: Este archivo NO debe subirse a Git
# Solo para desarrollo local si quieres probar con PostgreSQL

# Variables de entorno para PostgreSQL local (ejemplo)
# Descomenta y ajusta si tienes PostgreSQL instalado localmente

# POSTGRES_URL=postgresql://usuario:password@localhost:5432/clientes_db
# DATABASE_URL=postgresql://usuario:password@localhost:5432/clientes_db

# Para desarrollo local, usa app.py con SQLite
# Para producción en Vercel, usa app_postgres.py con estas variables (Vercel las provee automáticamente)
